package pass;

import java.lang.reflect.Array;
import java.util.Scanner;

public class search {

	/**
	 * 题目要求
	 *   请在一个类中编写一个方法，这个方法搜索一个字符数组中是否存在某个字符，
	 *   如果存在，则返回这个字符在字符数组中第一次出现的位置（序号从0开始计算），
	 *   否则，返回-1。要搜索的字符数组和字符都以参数形式传递传递给该方法，
	 *   如果传入的数组为null，应抛出IllegalArgumentException异常。在类的main方法中
	 *   以各种可能出现的情况测试验证该方法编写得是否正确，例如，字符不存在，字符存在，传入的数组为null等。
	 * 功能是返回输入的字符在数组中第一次出现的位置
	 * 怎么更好的接收一个输入的字符呢
	 * @param args
	 */
	public static void main(String[] args) {
		// TODO Auto-generated method stub
      char[] a={'a','b','c','d','e','f','g','h'};
      char[] b={'a','b','c','d','e','f','g','h'};




    
      
	}
  
	
	public void pick(char[] a,char ch){
		Scanner sc=new Scanner(System.in);
		String s=sc.nextLine();
		ch=s.charAt(0);
		
	}

}















